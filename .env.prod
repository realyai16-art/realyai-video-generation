# # Production environment file (example)
# # IMPORTANT: Keep this file secret. Do NOT commit real secrets to the repo.

# # Folders (production paths can be absolute or relative to the app root)
# UPLOAD_FOLDER=/var/app/uploads
# VIDEO_FOLDER=/var/app/generated
# METADATA_FOLDER=/var/app/metadata
# LOG_FILE=/var/log/realyai/generation_log.txt

# # Google Gemini / GenAI API key
# # Use a secrets manager if possible; set this via environment or systemd unit instead of committing.
# GEMINI_API_KEY=__REPLACE_WITH_REAL_GEMINI_API_KEY__

# # Database connection
# # Use a DSN/URL for production. Example:
# # postgresql://username:password@hostname:5432/databasename
# DATABASE_URL=postgresql://realyai:strongpassword@db-host.example.com:5432/realyai

# # If you prefer individual DB variables instead of DATABASE_URL, set these:
# DB_NAME=realyai
# DB_USER=realyai
# DB_PASSWORD=strongpassword
# DB_HOST=db-host.example.com
# DB_PORT=5432

# # Flask / App settings
# FLASK_ENV=production
# SECRET_KEY=__REPLACE_WITH_STRONG_SECRET_KEY__

# # Logging level
# LOG_LEVEL=INFO

# # Notes:
# # - For production, use a process manager (systemd, supervisord) and a WSGI server (Gunicorn, Waitress).
# # - Prefer storing secrets in a secret manager (AWS Secrets Manager, Azure Key Vault, GCP Secret Manager) or
# #   export them in the systemd unit file instead of keeping them in a file on disk.
# Database Configuration
DATABASE_URL="postgresql://realyai:Realyai@2025@34.135.55.17:5432/realyai"

# Google AI API Configuration
GEMINI_API_KEY="AIzaSyAWg2HFQ1td6Y6LUU816-KJbi5S6CL9iCk"

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=true

# Application Settings
UPLOAD_FOLDER=uploads
VIDEO_FOLDER=videos
DB_PATH=video_generation.db
LOG_FILE=generation_log.txt